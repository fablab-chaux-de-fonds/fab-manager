'use strict'
Application.Controllers.controller "PressController", ["$scope", ($scope) ->
  initialize = ->
    $scope.data = []

  ## !!! MUST BE CALLED AT THE END of the controller
  initialize()
]
